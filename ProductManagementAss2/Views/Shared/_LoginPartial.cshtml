@if (string.IsNullOrEmpty(User.Identity.Name))
{
    <ul class="navbar-nav">
        @if (@ViewContext.RouteData.Values["controller"].ToString() == "Home" ||  @ViewContext.RouteData.Values["action"].ToString() == "Login")
        {
            <li class="nav-item">
                <a class="nav-link text-light" asp-area="" asp-controller="UserAuthentication" asp-action="Registration">
                    <i class="bi bi-person-plus"></i> Register
                </a>
                
            </li>
        }
        @if (@ViewContext.RouteData.Values["controller"].ToString() == "Home" || @ViewContext.RouteData.Values["action"].ToString() == "Registration")
        {
            <li class="nav-item">   
                <a class="nav-link text-light" asp-area="" asp-controller="UserAuthentication" asp-action="Login">
                    <i class="bi bi-box-arrow-in-right"></i> Login
                </a>
        </li>
        }

    </ul>
 }
else
{
    <ul class="navbar-nav">
    <li class="nav-item">
        <span class="nav-link text-light">
            <i class="bi bi-person bi-5x"></i>Welcome @User.Identity.Name
        </span>
    </li>
    <li class="nav-item">
        <a class="nav-link text-light rounded" asp-area="" asp-controller="UserAuthentication" asp-action="Logout">
            <i class="bi bi-box-arrow-right"></i> 
        </a>
    </li>
</ul>

}